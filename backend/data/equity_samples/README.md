# Equity Bank sample fixtures (file-exchange integration)
#
# Recreated from samples Georgia shared (outbound batch + ACK/NACK/PSR).
# Used by scripts/prove-equity-response-parsers.ts — not live data.
#
# Outbound sample includes BT/MW/LBT/TT rows for format reference; our
# generator emits MW (M-Pesa) only.
