# Local dev — copy to mobile/.env (Expo loads EXPO_PUBLIC_* automatically)
EXPO_PUBLIC_API_URL=http://localhost:3001/api

# NEW App Supabase (Farmer + Agent + Banking) — NOT admin/Loveable platform
EXPO_PUBLIC_APP_SUPABASE_URL=https://tzaipijebibisgkwrdnz.supabase.co
EXPO_PUBLIC_APP_SUPABASE_ANON_KEY=

# hybrid = Supabase + local SQLite sync. api = API-only (web without Supabase keys).
EXPO_PUBLIC_SYNC_MODE=hybrid
